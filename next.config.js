/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/GlobalMarketAgent-AG',
  trailingSlash: true,
  images: { unoptimized: true },
}

module.exports = nextConfig
